# =============================================================================
# VWL Messaging — Environment Variables
# =============================================================================

# --- Railway auto-fills these (don't set manually on Railway) ---
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/vwl"
REDIS_URL="redis://localhost:6379"

# --- You MUST set these manually (in Railway dashboard → Variables) ---

# Twilio — get from https://console.twilio.com
TWILIO_ACCOUNT_SID="ACxxxxx"
TWILIO_AUTH_TOKEN="xxxxx"
TWILIO_PHONE_NUMBER="+1xxxxxxxxxx"

# SendGrid — get from https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY="SG.xxxxx"

# Anthropic — get from https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY="sk-ant-xxxxx"

# Escalation contact (Alex's phone + email for no-show follow-up)
ESCALATION_PHONE="+1xxxxxxxxxx"
ESCALATION_EMAIL="alex@valleyweightloss.com"

# App — set BASE_URL to your Railway public domain (e.g. https://vwl-messaging-production.up.railway.app)
BASE_URL="http://localhost:3000"

# --- Optional (have sensible defaults) ---

# Port — Railway sets this automatically, default 3000 for local
PORT=3000

# SendGrid sender identity
SENDGRID_FROM_EMAIL="care@valleyweightloss.com"
SENDGRID_FROM_NAME="Valley Weight Loss"

# Cal.com webhook secret — for verifying webhook signatures (skip for now)
# CALCOM_WEBHOOK_SECRET="xxxxx"
